---
phase: 13-shell-wrappers-npm-distribution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - npm/package.json
  - npm/platforms/darwin-arm64/package.json
  - npm/platforms/darwin-amd64/package.json
  - npm/platforms/linux-amd64/package.json
  - npm/platforms/linux-arm64/package.json
  - npm/bin/wt -> npm/bin/ptt (rename)
autonomous: true

must_haves:
  truths:
    - "Main npm package is named @a-tarek/ptt"
    - "Platform packages are named @a-tarek/ptt-{platform}-{arch}"
    - "npm bin field exposes ptt command (not wt)"
    - "Binary wrapper resolves @a-tarek/ptt-* platform packages"
    - "Binary wrapper file is npm/bin/ptt (not npm/bin/wt)"
    - "Repository URLs point to github:a-tarek/ptt"
    - "Package descriptions reference ptt brand"
  artifacts:
    - path: "npm/package.json"
      provides: "Main npm package definition"
      contains: "@a-tarek/ptt"
    - path: "npm/bin/ptt"
      provides: "Node.js binary wrapper"
      contains: "@a-tarek/ptt-darwin-arm64"
    - path: "npm/platforms/darwin-arm64/package.json"
      provides: "macOS ARM64 platform package"
      contains: "@a-tarek/ptt-darwin-arm64"
    - path: "npm/platforms/darwin-amd64/package.json"
      provides: "macOS x64 platform package"
      contains: "@a-tarek/ptt-darwin-amd64"
    - path: "npm/platforms/linux-amd64/package.json"
      provides: "Linux x64 platform package"
      contains: "@a-tarek/ptt-linux-amd64"
    - path: "npm/platforms/linux-arm64/package.json"
      provides: "Linux ARM64 platform package"
      contains: "@a-tarek/ptt-linux-arm64"
  key_links:
    - from: "npm/package.json"
      to: "npm/bin/ptt"
      via: "bin field mapping"
      pattern: '"ptt":\\s*"bin/ptt"'
    - from: "npm/package.json"
      to: "npm/platforms/*/package.json"
      via: "optionalDependencies"
      pattern: "@a-tarek/ptt-(darwin|linux)"
    - from: "npm/bin/ptt"
      to: "npm/platforms/*/bin/ptt"
      via: "require.resolve for platform binary"
      pattern: "require\\.resolve.*@a-tarek/ptt.*bin/ptt"
    - from: "scripts/publish-npm.sh"
      to: "npm/package.json"
      via: "version update and npm publish"
      pattern: "@a-tarek/ptt"
---

<objective>
Rebrand all npm package definitions and the binary wrapper from `@potato/wt` to `@a-tarek/ptt`.

Purpose: npm distribution is how users install ptt. Package names, bin fields, repository URLs, and the binary wrapper must all reference the new brand and scope so `npm install @a-tarek/ptt` works correctly.

Output: All 5 package.json files reference `@a-tarek/ptt` scope, binary wrapper file renamed to `npm/bin/ptt` with updated package references, and descriptions reflect the ptt brand.
</objective>

<execution_context>
@/Users/ahmed.tarek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmed.tarek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-shell-wrappers-npm-distribution/13-RESEARCH.md
@npm/package.json
@npm/bin/wt
@npm/platforms/darwin-arm64/package.json
@npm/platforms/darwin-amd64/package.json
@npm/platforms/linux-amd64/package.json
@npm/platforms/linux-arm64/package.json
@scripts/build-npm.sh
@scripts/publish-npm.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand npm package.json files</name>
  <files>
    npm/package.json
    npm/platforms/darwin-arm64/package.json
    npm/platforms/darwin-amd64/package.json
    npm/platforms/linux-amd64/package.json
    npm/platforms/linux-arm64/package.json
  </files>
  <action>
**npm/package.json (main package):**
Update to match this exact content:
```json
{
  "name": "@a-tarek/ptt",
  "version": "0.0.0",
  "description": "ptt: a potato worktree manager - fast, intuitive git worktree management",
  "repository": "github:a-tarek/ptt",
  "license": "MIT",
  "bin": { "ptt": "bin/ptt" },
  "files": ["bin/ptt"],
  "optionalDependencies": {
    "@a-tarek/ptt-darwin-arm64": "0.0.0",
    "@a-tarek/ptt-darwin-amd64": "0.0.0",
    "@a-tarek/ptt-linux-amd64": "0.0.0",
    "@a-tarek/ptt-linux-arm64": "0.0.0"
  },
  "engines": { "node": ">=18" }
}
```

**npm/platforms/darwin-arm64/package.json:**
```json
{
  "name": "@a-tarek/ptt-darwin-arm64",
  "version": "0.0.0",
  "description": "ptt: a potato worktree manager - macOS ARM64 binary",
  "repository": "github:a-tarek/ptt",
  "license": "MIT",
  "os": ["darwin"],
  "cpu": ["arm64"],
  "bin": { "ptt": "bin/ptt" },
  "files": ["bin/ptt"]
}
```

**npm/platforms/darwin-amd64/package.json:**
Same pattern — name: `@a-tarek/ptt-darwin-amd64`, description: `macOS x64 binary`, os: `darwin`, cpu: `x64`

**npm/platforms/linux-amd64/package.json:**
Same pattern — name: `@a-tarek/ptt-linux-amd64`, description: `Linux x64 binary`, os: `linux`, cpu: `x64`

**npm/platforms/linux-arm64/package.json:**
Same pattern — name: `@a-tarek/ptt-linux-arm64`, description: `Linux ARM64 binary`, os: `linux`, cpu: `arm64`

All platform packages share: `"repository": "github:a-tarek/ptt"`, `"bin": { "ptt": "bin/ptt" }`, `"files": ["bin/ptt"]`

**IMPORTANT:** Keep version at `0.0.0` — the publish-npm.sh script handles version updates at publish time.
  </action>
  <verify>
Run `node -e "const p = require('./npm/package.json'); console.log(p.name, p.bin, Object.keys(p.optionalDependencies))"` to verify main package name, bin field, and dependency names.
Run `grep -r "@potato" npm/` to confirm zero matches (all old scope references replaced).
Run `grep -r '"wt"' npm/` to confirm zero matches in bin fields and names (note: git "worktree" in descriptions is fine).
Verify each platform package.json has correct os/cpu fields and @a-tarek scope.
  </verify>
  <done>
All 5 package.json files use @a-tarek/ptt scope, bin fields reference ptt, descriptions reference "ptt: a potato worktree manager", repository points to github:a-tarek/ptt.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename and rebrand npm binary wrapper</name>
  <files>
    npm/bin/wt -> npm/bin/ptt (rename)
  </files>
  <action>
1. **Rename the file:** `npm/bin/wt` to `npm/bin/ptt` (use `git mv npm/bin/wt npm/bin/ptt`)

2. **Update the file content** with these specific changes:
   - Platform map values: `'@potato/wt-darwin-arm64'` to `'@a-tarek/ptt-darwin-arm64'` (and same for all 4 platform entries)
   - Platform map values use `amd64` not `x64` for the package name suffix (matching Go convention), but the map keys still use Node.js `x64` convention — this is correct and should NOT change
   - `require.resolve` path: `${packageName}/bin/wt` to `${packageName}/bin/ptt`
   - Error messages: `"Failed to execute wt:"` to `"Failed to execute ptt:"`
   - npm install suggestion: `"npm install ${packageName}"` stays the same (it already uses the variable)

3. **Verify the shebang** `#!/usr/bin/env node` is preserved on line 1.

The scripts/build-npm.sh and scripts/publish-npm.sh already reference `ptt` binary paths and `@a-tarek` scope (updated in Phase 11). No changes needed to those scripts.

The .github/workflows/release.yml already references `ptt` binary paths and `@a-tarek` scope. No changes needed.
  </action>
  <verify>
Run `test -f npm/bin/ptt && echo "exists" || echo "missing"` to confirm file was renamed.
Run `test ! -f npm/bin/wt && echo "removed" || echo "still exists"` to confirm old file is gone.
Run `head -1 npm/bin/ptt` to confirm shebang is preserved.
Run `grep -c "@a-tarek/ptt" npm/bin/ptt` to confirm new scope references (should be 4+ matches).
Run `grep "@potato" npm/bin/ptt` to confirm zero old scope matches.
Run `grep "bin/wt" npm/bin/ptt` to confirm zero old binary path matches.
Run `node -c npm/bin/ptt` to confirm valid JavaScript syntax.
  </verify>
  <done>
Binary wrapper file renamed to npm/bin/ptt, all platform package references use @a-tarek/ptt-* scope, require.resolve points to bin/ptt, error messages reference ptt, and the file is valid JavaScript.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -r "@potato" npm/` returns no matches — all old scope references gone
2. `grep -r '"wt":' npm/` returns no matches — no old bin field references
3. `grep -r "bin/wt" npm/` returns no matches — no old binary path references
4. `test -f npm/bin/ptt` — new wrapper file exists
5. `test ! -f npm/bin/wt` — old wrapper file removed
6. `node -c npm/bin/ptt` — valid JavaScript
7. `node -e "const p = require('./npm/package.json'); console.log(p.name)"` outputs `@a-tarek/ptt`
8. All 4 platform packages have correct @a-tarek scope names
9. `scripts/build-npm.sh` and `scripts/publish-npm.sh` already reference ptt (no changes needed — verify unchanged)
10. `.github/workflows/release.yml` already references ptt (no changes needed — verify unchanged)
</verification>

<success_criteria>
- Main package is `@a-tarek/ptt` with `"bin": { "ptt": "bin/ptt" }`
- All 4 platform packages are `@a-tarek/ptt-{platform}-{arch}` with `"bin": { "ptt": "bin/ptt" }`
- Binary wrapper is `npm/bin/ptt` (renamed from `npm/bin/wt`)
- Binary wrapper resolves `@a-tarek/ptt-*/bin/ptt` platform binaries
- Zero references to `@potato` or old `wt` binary names in npm directory
- Build and publish scripts need no changes (already rebranded)
</success_criteria>

<output>
After completion, create `.planning/phases/13-shell-wrappers-npm-distribution/13-02-SUMMARY.md`
</output>
