---
phase: 13-shell-wrappers-npm-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/shell/templates/wrapper.bash
  - internal/shell/templates/wrapper.zsh
  - internal/shell/templates/wrapper.fish
  - internal/shell/embed.go
  - internal/installer/rcfile.go
  - internal/installer/rcfile_test.go
  - tests/shell/shell_test.go
autonomous: true

must_haves:
  truths:
    - "Sourcing bash wrapper defines a ptt() function (not wt)"
    - "Sourcing zsh wrapper defines a ptt() function (not wt)"
    - "Sourcing fish wrapper defines a ptt function (not wt)"
    - "Shell wrapper intercepts go, goto, home, mk, new, and eject for cd behavior"
    - "RC file markers use >>> ptt >>> and <<< ptt <<< format"
    - "Eval line references ptt shell-init (not wt shell-init)"
    - "Backup files use .ptt-backup extension (not .wt-backup)"
    - "All existing tests pass with updated strings"
  artifacts:
    - path: "internal/shell/templates/wrapper.bash"
      provides: "Bash shell wrapper with ptt function name"
      contains: "ptt()"
    - path: "internal/shell/templates/wrapper.zsh"
      provides: "Zsh shell wrapper with ptt function name"
      contains: "ptt()"
    - path: "internal/shell/templates/wrapper.fish"
      provides: "Fish shell wrapper with ptt function name"
      contains: "function ptt"
    - path: "internal/shell/embed.go"
      provides: "Embed with __PTT_BIN__ placeholder"
      contains: "__PTT_BIN__"
    - path: "internal/installer/rcfile.go"
      provides: "RC file management with ptt markers"
      contains: ">>> ptt >>>"
    - path: "internal/installer/rcfile_test.go"
      provides: "Tests updated for ptt strings"
      contains: ">>> ptt >>>"
  key_links:
    - from: "internal/shell/embed.go"
      to: "internal/shell/templates/wrapper.bash"
      via: "go:embed directive and placeholder replacement"
      pattern: "__PTT_BIN__"
    - from: "internal/installer/rcfile.go"
      to: "cmd/install.go"
      via: "MarkerBegin/MarkerEnd constants and EvalLine function"
      pattern: "installer\\.MarkerBegin|installer\\.EvalLine"
---

<objective>
Rebrand all shell wrapper templates, the embed layer, and the installer's RC file management from `wt` to `ptt`.

Purpose: Shell integration is the user-facing entry point — sourcing the wrapper defines the `ptt()` function that intercepts cd-commands. RC file markers must match the new brand so install/uninstall work correctly.

Output: All three shell wrappers define `ptt()`, use `__PTT_BIN__` placeholder, intercept the restructured command names (go/goto/home/mk/new/eject), and RC file markers/eval lines reference `ptt`.
</objective>

<execution_context>
@/Users/ahmed.tarek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmed.tarek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-shell-wrappers-npm-distribution/13-RESEARCH.md
@internal/shell/templates/wrapper.bash
@internal/shell/templates/wrapper.zsh
@internal/shell/templates/wrapper.fish
@internal/shell/embed.go
@internal/installer/rcfile.go
@internal/installer/rcfile_test.go
@tests/shell/shell_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand shell wrapper templates and embed layer</name>
  <files>
    internal/shell/templates/wrapper.bash
    internal/shell/templates/wrapper.zsh
    internal/shell/templates/wrapper.fish
    internal/shell/embed.go
  </files>
  <action>
Update all three shell wrapper templates and the embed.go file:

**wrapper.bash and wrapper.zsh** (identical changes):
1. Rename function: `wt()` to `ptt()`
2. Update case statement: `goto|home|new|eject)` to `go|goto|home|mk|new|eject)` — this adds the new primary command names (`go`, `mk`) alongside the backward-compatible aliases (`goto`, `home`, `new`) so ALL cd-commands are intercepted regardless of whether user types `ptt go` or `ptt goto`
3. Replace all `"__WT_BIN__"` with `"__PTT_BIN__"` (3 occurrences per file: two in the case body, one in the completions eval)

**wrapper.fish:**
1. Rename function: `function wt` to `function ptt`
2. Update case statement: `case goto home new eject` to `case go goto home mk new eject`
3. Replace all `"__WT_BIN__"` with `"__PTT_BIN__"` (3 occurrences)

**embed.go:**
1. Change `const binPlaceholder = "__WT_BIN__"` to `const binPlaceholder = "__PTT_BIN__"`

No other changes in embed.go — the variable names (`wrapperBash`, `wrapperZsh`, `wrapperFish`) are internal and don't affect functionality.
  </action>
  <verify>
Run `go build ./...` to confirm compilation succeeds.
Run `grep -r "__WT_BIN__" internal/shell/` to confirm zero matches (all replaced).
Run `grep -r "wt()" internal/shell/templates/` to confirm zero matches.
Run `grep -c "ptt" internal/shell/templates/wrapper.bash` to confirm ptt references exist.
  </verify>
  <done>
All three wrapper templates define ptt function, use __PTT_BIN__ placeholder, and intercept go|goto|home|mk|new|eject commands. embed.go uses __PTT_BIN__ constant.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebrand RC file management and update all tests</name>
  <files>
    internal/installer/rcfile.go
    internal/installer/rcfile_test.go
    tests/shell/shell_test.go
  </files>
  <action>
**rcfile.go — update all wt references to ptt:**
1. `MarkerBegin`: `"# >>> wt >>>"` to `"# >>> ptt >>>"`
2. `MarkerEnd`: `"# <<< wt <<<"` to `"# <<< ptt <<<"`
3. Comments on MarkerBegin/MarkerEnd: `"wt configuration block"` to `"ptt configuration block"`
4. `EvalLine` fish case: `"wt shell-init | source"` to `"ptt shell-init | source"`
5. `EvalLine` default case: `eval "$(wt shell-init)"` to `eval "$(ptt shell-init)"`
6. `MarkerBlock` managed comment: `"Managed by wt installer"` to `"Managed by ptt installer"`
7. `CommentOutLines` prefix: `"# [wt v2 migration] "` to `"# [ptt migration] "` — the v2 migration label is confusing post-rebrand; use `ptt migration` instead since we're now ptt
8. `BackupFile` suffix: `".wt-backup"` to `".ptt-backup"`
9. The v1 detection patterns (`v1SourcePattern`, `v1DotPattern`) that match `wt.zsh` should remain unchanged — they still need to detect old wt v1 installs for migration purposes

**rcfile_test.go — update ALL test string literals to match new constants:**
1. All instances of `# >>> wt >>>` to `# >>> ptt >>>`
2. All instances of `# <<< wt <<<` to `# <<< ptt <<<`
3. All instances of `eval "$(wt shell-init)"` to `eval "$(ptt shell-init)"`
4. All instances of `wt shell-init | source` to `ptt shell-init | source`
5. All instances of `"Managed by wt installer"` to `"Managed by ptt installer"`
6. All instances of `"# [wt v2 migration] "` to `"# [ptt migration] "`

**tests/shell/shell_test.go — update binary name references:**
1. Change `"wt-test-*"` temp dir prefix to `"ptt-test-*"` (cosmetic but consistent)
2. Change binary output path from `"wt"` to `"ptt"` in `filepath.Join(tmpDir, "wt")` to `filepath.Join(tmpDir, "ptt")`
3. Update any error messages referencing `"wt binary"` to `"ptt binary"`

Search the entire file for any remaining `wt` references that should be `ptt` (but leave git-related `worktree` references alone — those are git concepts, not brand references).
  </action>
  <verify>
Run `go test ./internal/installer/...` — all tests must pass.
Run `go test ./tests/shell/...` — all tests must pass (note: shell tests may need a git repo context; if they fail due to environment, that's OK — verify the string changes are correct).
Run `go test ./...` — full test suite passes.
Run `grep -r ">>> wt >>>" internal/installer/` to confirm zero matches.
Run `grep -r "wt shell-init" internal/installer/` to confirm zero matches.
Run `grep -r "\.wt-backup" internal/installer/` to confirm zero matches.
  </verify>
  <done>
RC file markers use `>>> ptt >>>` / `<<< ptt <<<`, eval lines reference `ptt shell-init`, backup files use `.ptt-backup`, migration comment uses `[ptt migration]`, and all tests pass with the updated strings.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `go build ./...` succeeds — project compiles
2. `go test ./...` passes — all tests green
3. `grep -rn ">>> wt >>>" internal/` returns no matches
4. `grep -rn "__WT_BIN__" internal/` returns no matches
5. `grep -rn "wt()" internal/shell/templates/` returns no matches
6. `grep -rn "\.wt-backup" internal/` returns no matches
7. `grep -rn "wt shell-init" internal/installer/` returns no matches
8. The v1 migration patterns in rcfile.go still reference `wt.zsh` (intentionally preserved for backward compat)
</verification>

<success_criteria>
- Shell wrappers define `ptt()` function with `__PTT_BIN__` placeholder
- Shell wrappers intercept all cd-commands: go, goto, home, mk, new, eject
- RC file markers are `# >>> ptt >>>` and `# <<< ptt <<<`
- Eval lines are `eval "$(ptt shell-init)"` and `ptt shell-init | source`
- Backup suffix is `.ptt-backup`
- All Go tests pass (`go test ./...`)
</success_criteria>

<output>
After completion, create `.planning/phases/13-shell-wrappers-npm-distribution/13-01-SUMMARY.md`
</output>
