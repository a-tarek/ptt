---
phase: 01-internal-documentation-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/CONCERNS.md
autonomous: true

must_haves:
  truths:
    - "CONVENTIONS.md documents flag parsing pattern used in _wt_new and _wt_eject"
    - "CONVENTIONS.md describes override mechanism as a coding pattern"
    - "CONCERNS.md updated to reflect variable rename (path → entry in _wt_setup)"
    - "CONCERNS.md removes stale concerns about old hardcoded logic"
  artifacts:
    - path: ".planning/codebase/CONVENTIONS.md"
      provides: "Flag parsing conventions and override pattern documentation"
      contains: "--copy"
      contains: "--symlink"
      contains: "while.*--\\*"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "Updated concerns reflecting current implementation"
      contains: "entry"
      min_lines: 100
  key_links:
    - from: "CONVENTIONS.md flag parsing pattern"
      to: "wt.zsh _wt_new and _wt_eject"
      via: "documented pattern matches implementation"
      pattern: "while.*\\$1.*==.*--\\*"
    - from: "CONCERNS.md variable scope section"
      to: "wt.zsh _wt_setup"
      via: "documents path→entry rename"
      pattern: "entry.*reserved"
---

<objective>
Update CONVENTIONS.md to document the new flag parsing pattern and override mechanism, and update CONCERNS.md to reflect current implementation (variable renames, removed hardcoded logic).

Purpose: CONVENTIONS guides future code additions to follow established patterns. CONCERNS warns about fragile areas and known issues to prevent regressions.

Output: Two refreshed documentation files matching current wt.zsh conventions and actual concerns.
</objective>

<execution_context>
@/Users/ahmed.tarek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmed.tarek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ahmed.tarek/code/wt/.planning/PROJECT.md
@/Users/ahmed.tarek/code/wt/.planning/ROADMAP.md
@/Users/ahmed.tarek/code/wt/.planning/STATE.md
@/Users/ahmed.tarek/code/wt/.planning/phases/01-internal-documentation-refresh/01-CONTEXT.md
@/Users/ahmed.tarek/code/wt/wt.zsh
</context>

<tasks>

<task type="auto">
  <name>Update CONVENTIONS.md with flag parsing and override patterns</name>
  <files>.planning/codebase/CONVENTIONS.md</files>
  <action>
Read current wt.zsh and update CONVENTIONS.md to reflect:

**Add new section: "Flag Parsing Pattern"**

Under "Code Style" or as new top-level section:

Pattern used in _wt_new (lines 37-44) and _wt_eject (lines 147-154):
```zsh
local -a overrides
while [[ "$1" == --* ]]; do
  case "$1" in
    --copy)   overrides+=("copy:$2"); shift 2 ;;
    --symlink) overrides+=("symlink:$2"); shift 2 ;;
    *) echo "Unknown flag: $1"; return 1 ;;
  esac
done
```

Document:
- Use `local -a arrayname` for flag accumulation
- Loop `while [[ "$1" == --* ]]` to consume all flags before positional args
- Build override strings in "action:path" format
- Shift 2 for flags with values, shift 1 for boolean flags
- Always include `*)` case for unknown flags with error message
- Prescriptive: "New commands with flags should follow this pattern"

**Add new section: "Override Mechanism Pattern"**

Document _wt_setup pattern (lines 368-444):
- Function signature accepting varargs: `function _wt_setup() { local src="$1" target="$2"; shift 2; ... }`
- Build associative array from varargs: `local -A overrides; for ov in "$@"; do ... done`
- Parse "action:path" format: `ov_action="${ov%%:*}"` and `ov_path="${ov#*:}"`
- Track applied overrides: `local -A applied` to prevent double-processing
- Two-phase application: (1) config file with override precedence, (2) one-off overrides not in config
- Prescriptive: "Setup functions should accept override args and apply them with precedence over config defaults"

**Update existing sections:**

In "Variables" section, add:
- Associative arrays use `local -A name` declaration: `overrides`, `applied` in _wt_setup
- Array iteration uses zsh `for item in "$@"` pattern
- Reserved variable avoidance: Use `entry` not `path` (zsh reserved in some contexts)

In "Function Design" → "Parameters" section:
- Update to mention varargs pattern for overrides: Functions accepting variable overrides use `shift N` then `"$@"` for remaining args

**Remove outdated content:**
- Delete any references to --copy-node-modules flag (replaced by --copy)
- Delete references to hardcoded .env.local or node_modules handling

Follow user decision: Both descriptive and prescriptive. Describe the pattern, then state the rule it implies for future code.
  </action>
  <verify>
Read updated CONVENTIONS.md and check:
- grep "while.*--\\*" to find flag parsing pattern
- grep "override" to find override mechanism section
- grep "local -A" to find associative array convention
- grep "entry" to find reserved variable avoidance note
- Verify no references to --copy-node-modules
  </verify>
  <done>
CONVENTIONS.md documents flag parsing pattern with while loop and case statement, override mechanism with associative arrays, reserved variable avoidance (entry not path), and removes stale --copy-node-modules references.
  </done>
</task>

<task type="auto">
  <name>Update CONCERNS.md to reflect current implementation</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
Read current wt.zsh and update CONCERNS.md to reflect:

**Update existing sections:**

In "Tech Debt" → "Variable Scope Leak in Parsing Loop":
- Line references: Update from 270-284 to 288-302 (_wt_list function moved)
- Variable name: Still uses `wt_entry` and `branch`, concern remains valid

In "Fragile Areas" → "Variable Scope" or similar:
- ADD new note: "Reserved Variable Collision Avoided" — Variable `path` renamed to `entry` in _wt_setup (line 393) to avoid zsh reserved word `path`. This was a preventive fix after discovering zsh uses `path` internally. Future code should avoid `path` as variable name.

**Remove outdated concerns:**

Delete or mark as RESOLVED:
- Any concerns about hardcoded .env.local copying (now handled by .wtconfig)
- Any concerns about hardcoded node_modules symlinking (now configurable)
- Concerns about --copy-node-modules flag (flag removed, replaced with --copy)

**Update line references throughout:**

Functions moved, so update line numbers in all sections:
- _wt_new: now lines 36-86 (was 34-100)
- _wt_eject: now lines 146-276 (was 129-258)
- _wt_setup: now lines 368-444 (completely rewritten, old refs invalid)
- _wt_list: now lines 278-310 (was 260-292)
- _wt_resolve_path: now lines 446-460 (was 352-364)
- _wt_resolve_branch: now lines 462-480 (was 367-384)
- _wt_list_names: now lines 482-505 (was 387-409)

**Add new concerns (if applicable):**

If override mechanism introduces concerns:
- "Override Precedence Complexity" — The two-phase override application in _wt_setup (config + one-offs) requires careful understanding. If config order matters, this could be confusing. Document that overrides always win over config defaults.

**Update file paths in headers:**
- File references should point to correct line ranges in wt.zsh (551 lines total)

Follow user decision: Keep concerns focused on actual current issues, remove resolved ones, update line ranges to match current code.
  </action>
  <verify>
Read updated CONCERNS.md and verify:
- grep "entry" to find reserved variable note
- grep "368-444" to confirm _wt_setup line range updated
- grep "path" to ensure old variable scope concern removed or updated
- Verify no concerns about --copy-node-modules
- Check that line ranges throughout file are updated
  </verify>
  <done>
CONCERNS.md reflects current implementation with updated line ranges, documents path→entry variable rename, removes concerns about removed hardcoded logic, and adds any new concerns from override mechanism.
  </done>
</task>

</tasks>

<verification>
After task completion:

1. Verify CONVENTIONS.md includes flag parsing pattern with while loop
2. Verify CONVENTIONS.md documents override mechanism with associative arrays
3. Verify CONCERNS.md has updated line ranges matching 551-line file
4. Verify CONCERNS.md mentions entry variable (avoiding reserved path)
5. Verify both files have no references to --copy-node-modules or hardcoded logic
</verification>

<success_criteria>
- CONVENTIONS.md documents flag parsing and override patterns with prescriptive rules
- CONVENTIONS.md notes reserved variable avoidance (entry not path)
- CONCERNS.md line ranges updated to match current wt.zsh
- CONCERNS.md removes stale concerns, adds note about path→entry rename
- Both files accurate to current 551-line implementation
</success_criteria>

<output>
After completion, create `.planning/phases/01-internal-documentation-refresh/01-02-SUMMARY.md`
</output>
