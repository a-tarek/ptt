---
phase: 01-internal-documentation-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
autonomous: true

must_haves:
  truths:
    - "ARCHITECTURE.md documents all 15+ functions currently in wt.zsh including _wt_init and _wt_setup"
    - "ARCHITECTURE.md shows call chains like wt new → _wt_setup → reads .wtconfig → applies overrides"
    - "STRUCTURE.md line ranges match current wt.zsh layout (551 lines total)"
    - "Both files describe current override mechanism (--copy/--symlink flags)"
  artifacts:
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "Complete function catalog with data flows and override mechanism"
      contains: "_wt_init"
      contains: "_wt_setup"
      contains: "override"
    - path: ".planning/codebase/STRUCTURE.md"
      provides: "Accurate line ranges for all sections"
      contains: "551"
      contains: "_wt_init"
      contains: "_wt_setup"
  key_links:
    - from: "ARCHITECTURE.md Data Flow sections"
      to: "wt.zsh actual implementation"
      via: "documented function calls match code"
      pattern: "_wt_setup.*overrides"
    - from: "STRUCTURE.md line ranges"
      to: "wt.zsh line numbers"
      via: "exact line number references"
      pattern: "lines \\d+-\\d+"
---

<objective>
Update ARCHITECTURE.md and STRUCTURE.md to reflect all current wt.zsh features, including new _wt_init function, rewritten _wt_setup with override mechanism, --copy/--symlink flags, and accurate line ranges.

Purpose: These are the foundational codebase map files that future AI sessions and human contributors rely on to understand the system's organization and behavior.

Output: Two fully refreshed documentation files matching current wt.zsh (551 lines).
</objective>

<execution_context>
@/Users/ahmed.tarek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmed.tarek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ahmed.tarek/code/wt/.planning/PROJECT.md
@/Users/ahmed.tarek/code/wt/.planning/ROADMAP.md
@/Users/ahmed.tarek/code/wt/.planning/STATE.md
@/Users/ahmed.tarek/code/wt/.planning/phases/01-internal-documentation-refresh/01-CONTEXT.md
@/Users/ahmed.tarek/code/wt/wt.zsh
</context>

<tasks>

<task type="auto">
  <name>Update ARCHITECTURE.md with new functions and override mechanism</name>
  <files>.planning/codebase/ARCHITECTURE.md</files>
  <action>
Read current wt.zsh (551 lines) and update ARCHITECTURE.md to reflect:

**New Function (_wt_init):**
- Add _wt_init to Command Handlers layer (lines 115-144)
- Purpose: Scaffold .wtconfig template file
- Behavior: Creates template with commented examples for Node.js, Python, Rust
- Error handling: Validates git repo, checks if .wtconfig already exists

**Rewritten Function (_wt_setup):**
- Update _wt_setup documentation (lines 368-444)
- NEW signature: `_wt_setup <src_root> <target_abs> [overrides...]`
- NEW behavior: Accepts override args in "action:path" format
- NEW implementation: Uses associative arrays (overrides, applied)
- NEW logic: Reads .wtconfig, applies overrides to config entries, handles one-off overrides not in config
- REMOVED: No hardcoded .env.local or node_modules logic
- Call chain: _wt_new → _wt_setup (with overrides array), _wt_eject → _wt_setup (with overrides array)

**Updated Functions (_wt_new, _wt_eject):**
- Both now parse --copy and --symlink flags (NOT --copy-node-modules)
- Flag parsing: `while [[ "$1" == --* ]]` loop with case statement
- Build overrides array: `overrides+=("action:path")`
- Pass to _wt_setup: `"${overrides[@]}"`

**Updated Data Flows:**
Replace "Worktree Creation Flow" to show:
1. User: `wt new [--copy <path>] [--symlink <path>] <name> [branch]`
2. Flag parsing: Build overrides array
3. Git worktree add
4. Call _wt_setup with src_root, target_abs, overrides
5. _wt_setup reads .wtconfig, applies actions with override precedence

Replace "Branch Ejection Flow" similarly.

**Updated Line References:**
- Main dispatcher: lines 4-34 (was 4-31)
- Command handlers: lines 36-364 (expanded from 34-346)
- _wt_init: lines 115-144
- _wt_setup: lines 368-444 (was simple copy logic)
- Helpers: lines 446-505
- Completions: lines 507-550

**Removed Patterns:**
- Delete references to hardcoded .env.local copying
- Delete references to --copy-node-modules flag
- Delete references to hardcoded node_modules symlinking

Follow user decision: Include full call chains showing how _wt_new → _wt_setup → reads .wtconfig → applies overrides. Show both descriptive (what it does) and prescriptive (pattern to follow) documentation.
  </action>
  <verify>
Read updated ARCHITECTURE.md and verify:
- _wt_init appears in Command Handlers section with lines 115-144
- _wt_setup described with override mechanism and associative arrays
- Data flow sections show override array being built and passed
- No references to --copy-node-modules or hardcoded .env.local logic
- Line ranges updated to match 551-line file
  </verify>
  <done>
ARCHITECTURE.md accurately documents all 15+ functions with correct line ranges, includes _wt_init and rewritten _wt_setup, shows override mechanism in data flows, and removes stale hardcoded patterns.
  </done>
</task>

<task type="auto">
  <name>Update STRUCTURE.md with accurate line ranges and new sections</name>
  <files>.planning/codebase/STRUCTURE.md</files>
  <action>
Read current wt.zsh (551 lines) and update STRUCTURE.md to reflect:

**File Line Count:**
- Update from ~450 lines to 551 lines

**Code Organization Within wt.zsh:**
Update section order and line ranges:

1. Shebang & Header (lines 1-2) — unchanged
2. Main Dispatcher (lines 4-34) — updated from 4-32
3. Command Handlers (lines 36-364) — expanded, now includes:
   - _wt_new(): lines 36-86 (updated from 34-100, now with flag parsing)
   - _wt_goto(): lines 88-103 (updated from 102-117)
   - _wt_home(): lines 105-113 (updated from 119-127)
   - _wt_init(): lines 115-144 (NEW function, not in old docs)
   - _wt_eject(): lines 146-276 (updated from 129-258, now with flag parsing)
   - _wt_list(): lines 278-310 (updated from 260-292)
   - _wt_merge(): lines 312-328 (updated from 294-310)
   - _wt_rebase(): lines 330-346 (updated from 312-328)
   - _wt_delete(): lines 348-364 (updated from 330-346)
4. Helper Functions (lines 366-505) — expanded:
   - _wt_setup(): lines 368-444 (NEW comprehensive version with override logic)
   - _wt_resolve_path(): lines 446-460 (updated from 352-364)
   - _wt_resolve_branch(): lines 462-480 (updated from 367-384)
   - _wt_list_names(): lines 482-505 (updated from 387-409)
5. Zsh Completions (lines 507-550) — updated from 411-446:
   - _wt(): lines 509-548 (updated, now includes --copy/--symlink for new and eject)
   - compdef: line 550 (updated from 446)

**Update "Where to Add New Code" section:**
- New subcommand example: Add before line 366 (was 411)
- Helpers section: lines 366-505 (was 348-409)
- Completions section: lines 507-550 (was 411-446)

**Add note about .wtconfig:**
In "Key File Locations" or "Configuration" section, mention:
- `.wtconfig` file at repo root (user-created via `wt init`)
- Format: `<action> <path>` per line (copy or symlink)
- Read by _wt_setup during worktree creation

Follow user decision: Keep it skimmable with clear headings. Use exact line ranges from current code.
  </action>
  <verify>
Read updated STRUCTURE.md and check:
- grep "551" to confirm total line count updated
- grep "_wt_init" to confirm new function documented
- grep "lines 368-444" to confirm _wt_setup range
- grep "lines 507-550" to confirm completions range
- Verify all command handler line ranges match wt.zsh
  </verify>
  <done>
STRUCTURE.md line ranges match current 551-line wt.zsh, includes _wt_init in command handlers section, documents _wt_setup in helpers with correct range, and updates all section boundaries.
  </done>
</task>

</tasks>

<verification>
After task completion:

1. Read both updated files and cross-reference against wt.zsh
2. Verify _wt_init documented in both files
3. Verify _wt_setup override mechanism explained in ARCHITECTURE.md
4. Verify all line ranges in STRUCTURE.md are accurate
5. Verify no stale references to --copy-node-modules or hardcoded .env.local logic
</verification>

<success_criteria>
- ARCHITECTURE.md contains all 15+ functions with _wt_init and rewritten _wt_setup
- ARCHITECTURE.md shows override mechanism in data flow sections
- STRUCTURE.md line ranges match current wt.zsh (551 lines)
- Both files follow dual-audience tone (AI-parseable, human-readable)
- No references to removed features (--copy-node-modules, hardcoded file operations)
</success_criteria>

<output>
After completion, create `.planning/phases/01-internal-documentation-refresh/01-01-SUMMARY.md`
</output>
