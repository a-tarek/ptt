---
phase: 02-user-facing-documentation
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [README.md]
autonomous: true

must_haves:
  truths:
    - "User can find usage for wt merge, wt rebase, and wt delete"
    - "User understands .wtconfig format with syntax, actions, and comments"
    - "User understands override flag precedence and one-off usage"
    - "User knows container/Docker workflows with port overrides and .env handling"
  artifacts:
    - path: "README.md"
      provides: "Remaining commands, configuration section, workflow section"
      contains: "## Configuration"
  key_links:
    - from: "README.md configuration section"
      to: "README.md wt new section"
      via: "cross-reference explaining .wtconfig is used by wt new and wt eject"
      pattern: "\\.wtconfig"
    - from: "README.md workflow section"
      to: "README.md override flags"
      via: "practical examples using --copy and --symlink"
      pattern: "--copy|--symlink"
---

<objective>
Add remaining command reference (merge, rebase, delete), configuration section (.wtconfig + overrides), and container/Docker workflow section to README.md.

Purpose: Complete the README with all remaining requirements so every wt feature is documented.
Output: README.md fully complete with DOC-06, DOC-08, DOC-09, DOC-10.
</objective>

<execution_context>
@/Users/ahmed.tarek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmed.tarek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@wt.zsh
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add merge, rebase, delete commands and configuration section</name>
  <files>README.md</files>
  <action>
Insert the remaining command sections and a new Configuration section into README.md.

**Part A: Insert command subsections AFTER `### wt list` and BEFORE `## Tab Completion`.**

**`wt merge` (DOC-06):**
- Subsection: `### wt merge`
- Usage: `wt merge <worktree>`
- Description: Merge the specified worktree's branch into the current branch. Resolves the worktree name to its branch, then runs `git merge`.
- Example:
  ```zsh
  # From main worktree, merge feature-auth's branch
  wt merge feature-auth
  ```

**`wt rebase` (DOC-06):**
- Subsection: `### wt rebase`
- Usage: `wt rebase <worktree>`
- Description: Rebase the current branch onto the specified worktree's branch. Resolves the worktree name to its branch, then runs `git rebase`.
- Example:
  ```zsh
  # From feature branch, rebase onto main
  wt rebase main
  ```

**`wt delete` (DOC-06):**
- Subsection: `### wt delete`
- Usage: `wt delete <worktree>`
- Description: Remove a worktree directory. The branch is kept — only the worktree checkout is removed. Uses `git worktree remove` under the hood.
- Example:
  ```zsh
  wt delete feature-auth
  ```

**Part B: Add `## Configuration` section AFTER the last command subsection (`### wt delete`) and BEFORE `## Tab Completion`.**

**`.wtconfig` Format (DOC-08):**
- Subsection: `### .wtconfig`
- Description: A configuration file at the repository root that defines files to copy or symlink into new worktrees. Created with `wt init`.
- Syntax:
  ```
  # Comment lines start with #
  <action> <path>
  ```
- Actions: `copy` (duplicate the file/directory) or `symlink` (create a symbolic link to the source).
- Full example:
  ```
  # .wtconfig — files to copy or symlink into new worktrees

  # Environment files (copy to allow per-worktree changes)
  copy .env
  copy .env.local

  # Dependencies (symlink to save disk space)
  symlink node_modules
  symlink .venv
  ```
- Explain when to use copy vs symlink:
  - **copy**: Files that differ between worktrees (environment variables, local config). Each worktree gets its own independent copy.
  - **symlink**: Large directories shared across worktrees (dependencies, build caches). Saves disk space but changes affect all worktrees.

**Override Flags (DOC-09):**
- Subsection: `### Override Flags`
- Description: The `--copy` and `--symlink` flags on `wt new` and `wt eject` override `.wtconfig` defaults for specific paths. They can also specify paths not in `.wtconfig` for one-off operations.
- Precedence: CLI flags > .wtconfig defaults
- Example scenarios:
  ```zsh
  # .wtconfig says "symlink .env" but you need a separate copy this time
  wt new feature-auth --copy .env

  # .wtconfig doesn't mention node_modules, but symlink it for this worktree
  wt new feature-auth --symlink node_modules

  # Override multiple paths
  wt new feature-auth --copy .env --copy .env.local --symlink node_modules
  ```
- Explain the two-phase mechanism briefly: (1) .wtconfig entries are processed with flag overrides taking precedence, (2) flag-only paths not in .wtconfig are applied as one-offs.
  </action>
  <verify>
- README.md contains "### wt merge", "### wt rebase", "### wt delete" sections
- README.md contains "## Configuration" section
- README.md contains "### .wtconfig" subsection with syntax and copy vs symlink explanation
- README.md contains "### Override Flags" subsection with precedence explanation
- grep "## Configuration" README.md succeeds
- grep "### Override Flags" README.md succeeds
  </verify>
  <done>
Remaining commands documented (DOC-06): merge, rebase, delete. Configuration fully documented (DOC-08 + DOC-09): .wtconfig format with copy/symlink guidance, override flag precedence with examples.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add container/Docker workflow section</name>
  <files>README.md</files>
  <action>
Insert a `## Workflows` section into README.md AFTER `## Configuration` and BEFORE `## Tab Completion`.

**Container/Docker Workflow (DOC-10):**
- Subsection: `### Working with Containers`
- Context: When using wt with Dockerized applications (docker-compose projects, etc.), each worktree may need its own container configuration. Explain practical patterns.

**Port Overrides:**
- Problem: Multiple worktrees running the same docker-compose service will conflict on port bindings.
- Solution: Copy `.env` (or docker-compose override file) per worktree so each can bind different ports.
- Example:
  ```
  # .wtconfig
  copy .env
  copy docker-compose.override.yml
  ```
  Then edit the copied `.env` in each worktree to use different ports.

**Environment Files:**
- Problem: Each worktree may need different environment variables (database names, API keys for different environments).
- Solution: Use `copy` action for `.env` files so each worktree gets independent configuration.
- Contrast with `symlink`: Symlinked .env means all worktrees share the same config — usually NOT what you want.

**Named Volumes:**
- Problem: Docker named volumes are global. Multiple worktrees using the same volume name will share data unexpectedly.
- Solution: Use worktree-specific volume names in the copied docker-compose override, or use relative bind mounts that naturally isolate per worktree directory.

**Example workflow:**
```zsh
# Set up .wtconfig for Docker projects
wt init
# Edit .wtconfig:
#   copy .env
#   copy docker-compose.override.yml
#   symlink node_modules

# Create worktree with isolated config
wt new feature-payments

# Edit .env in the new worktree to use different ports
# APP_PORT=3001 (instead of 3000)
# DB_PORT=5433 (instead of 5432)

# Start containers in the new worktree
docker-compose up -d
```

Keep this section practical and brief — 3 subsections (ports, env files, volumes) with concrete examples. This is advice, not a Docker tutorial.
  </action>
  <verify>
- README.md contains "## Workflows" section
- README.md contains "### Working with Containers" subsection
- Section mentions port overrides, .env handling, and named volumes
- grep "### Working with Containers" README.md succeeds
- grep "docker-compose" README.md succeeds (at least one docker reference)
  </verify>
  <done>
Container/Docker workflow documented (DOC-10): port override strategy, .env copy rationale, named volume isolation. Users know how to configure wt for Dockerized projects.
  </done>
</task>

</tasks>

<verification>
- README.md is complete with ALL sections: header, installation, quick start, commands (all 9), configuration, workflows, tab completion
- Every DOC requirement (DOC-01 through DOC-11) is covered
- Section order is logical: Installation → Quick Start → Commands → Configuration → Workflows → Tab Completion
- No placeholder or stub sections remain
- All code examples are syntactically valid
- Cross-references between sections are accurate (e.g., "see Configuration" from wt new)
</verification>

<success_criteria>
- DOC-06 (merge, rebase, delete): All three commands documented with examples
- DOC-08 (.wtconfig format): Syntax explained, copy vs symlink guidance provided
- DOC-09 (override flags): Precedence explained, one-off usage shown
- DOC-10 (container workflows): Port overrides, .env handling, named volumes covered
- README.md is complete — no further content additions needed
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-facing-documentation/02-03-SUMMARY.md`
</output>
