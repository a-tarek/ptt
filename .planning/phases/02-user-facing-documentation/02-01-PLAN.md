---
phase: 02-user-facing-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true

must_haves:
  truths:
    - "User can clone the repo and source wt.zsh following README instructions"
    - "User can see what wt is and what it does from the README header"
    - "User knows about tab completion and how to initialize a config"
  artifacts:
    - path: "README.md"
      provides: "Project introduction, installation, quick start, init command, completion note"
      contains: "source.*wt.zsh"
  key_links:
    - from: "README.md"
      to: "wt.zsh"
      via: "source command in installation instructions"
      pattern: "source.*wt\\.zsh"
---

<objective>
Create README.md with project header, installation instructions, quick start guide, `wt init` documentation, and tab completion note.

Purpose: Establish the README foundation that subsequent plans will extend with command reference and workflow sections.
Output: README.md with installation (DOC-01, DOC-02), init command (DOC-07), and completion mention (DOC-11).
</objective>

<execution_context>
@/Users/ahmed.tarek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmed.tarek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@wt.zsh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md with header, installation, quick start, init, and completion</name>
  <files>README.md</files>
  <action>
Create README.md at the repository root with these sections:

**1. Header / Project Title**
- Title: `# wt — Git Worktree Manager`
- One-line description: A zsh tool that manages git worktrees with shared configuration, file copying/symlinking, and branch ejection.
- Brief feature highlights (bullet list): create worktrees with shared config, copy or symlink files between worktrees, eject branches into dedicated worktrees, navigate between worktrees, merge/rebase across worktrees.

**2. Installation (DOC-01 + DOC-02)**
- Section: `## Installation`
- Clone instructions: `git clone https://github.com/aatarek/wt.git`
- Source-in-zshrc: Add `source /path/to/wt/wt.zsh` to `~/.zshrc`
- Show complete example:
  ```zsh
  # Clone the repository
  git clone https://github.com/aatarek/wt.git ~/wt

  # Add to your .zshrc
  echo 'source ~/wt/wt.zsh' >> ~/.zshrc

  # Reload shell
  source ~/.zshrc
  ```
- Note: Requires zsh and git (with worktree support, git 2.5+).

**3. Quick Start**
- Section: `## Quick Start`
- Show a brief 4-5 line workflow demonstrating the core loop:
  ```
  wt init                          # Create .wtconfig in your repo
  wt new feature-login             # Create worktree + branch
  wt goto feature-login            # Jump to a worktree
  wt home                          # Return to main worktree
  wt delete feature-login          # Clean up when done
  ```
- Brief explanation of what each command does (1 line each).

**4. Commands Overview**
- Section: `## Commands`
- Show the usage synopsis from `wt` (no args) output exactly as it appears in the dispatcher (lines 19-31 of wt.zsh).
- Add a note: "Detailed usage for each command follows below." (This sets up for Plan 02 and 03 to add detailed subsections.)

**5. `wt init` (DOC-07)**
- Subsection: `### wt init`
- Usage: `wt init`
- Description: Creates a `.wtconfig` template in the repository root with commented examples for common setups (Node.js, Python, Rust).
- Mention: Only creates the file if it doesn't already exist. The file is used by `wt new` and `wt eject` to automatically copy or symlink files into new worktrees.
- Show the default template content that `wt init` generates (from lines 128-142 of wt.zsh).

**6. Tab Completion (DOC-11)**
- Section: `## Tab Completion`
- Brief note: wt includes built-in zsh tab completion. After sourcing wt.zsh, press Tab after `wt` to see available commands, and after `wt goto`, `wt merge`, `wt rebase`, or `wt delete` to complete worktree names. The `wt new` and `wt eject` commands also complete `--copy` and `--symlink` flags with file paths.
- Note: Completion is automatically registered via `compdef` when wt.zsh is sourced. No additional setup needed.

**Style guidelines:**
- Use code blocks with `zsh` language tag for shell examples.
- Keep descriptions concise and practical — this is a CLI tool README, not a tutorial.
- Use consistent heading hierarchy: `##` for major sections, `###` for commands.
- Do NOT add a table of contents (file is small enough to scroll).
- Do NOT add badges, shields, or CI status (none exist).
- End the file with a blank line.
  </action>
  <verify>
- File README.md exists at repository root
- Contains "## Installation" section with clone and source instructions
- Contains "source" command referencing wt.zsh
- Contains "## Quick Start" section
- Contains "### wt init" subsection
- Contains "## Tab Completion" section
- grep -c "source" README.md returns at least 2 (install example + zshrc reload)
  </verify>
  <done>
README.md exists with header, installation (DOC-01 + DOC-02), quick start, commands overview, wt init (DOC-07), and tab completion (DOC-11). A new user can read this file and successfully install and start using wt.
  </done>
</task>

</tasks>

<verification>
- README.md exists at repo root
- Installation instructions reference `source wt.zsh` in .zshrc
- Quick start shows core workflow (init, new, goto, home, delete)
- `wt init` command is fully documented with template output
- Tab completion section explains what completes and that no setup is needed
- All code examples use `zsh` language tag
- No placeholder sections (everything in this plan is complete content)
</verification>

<success_criteria>
- DOC-01 (clone/download): README has git clone instruction
- DOC-02 (source-in-zshrc): README has source command with example
- DOC-07 (wt init): README has init subsection with template content
- DOC-11 (tab completion): README has completion section
- A new user reading only these sections can install wt and create their first .wtconfig
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-facing-documentation/02-01-SUMMARY.md`
</output>
