---
phase: 02-user-facing-documentation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [README.md]
autonomous: true

must_haves:
  truths:
    - "User can find complete usage for wt new with all flags and examples"
    - "User can find complete usage for wt eject with all flags and examples"
    - "User can find usage for wt goto, wt home, and wt list"
  artifacts:
    - path: "README.md"
      provides: "Detailed command reference for new, eject, goto, home, list"
      contains: "### wt new"
  key_links:
    - from: "README.md wt new section"
      to: "README.md .wtconfig section (plan 03)"
      via: "cross-reference to configuration section"
      pattern: "\\.wtconfig"
---

<objective>
Add detailed command reference sections for `wt new`, `wt eject`, `wt goto`, `wt home`, and `wt list` to README.md.

Purpose: Document the primary worktree creation and navigation commands so users understand all flags, positional args, and behavior.
Output: README.md extended with DOC-03 (wt new), DOC-04 (wt eject), DOC-05 (wt goto, wt home, wt list).
</objective>

<execution_context>
@/Users/ahmed.tarek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahmed.tarek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@wt.zsh
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add wt new and wt eject command reference</name>
  <files>README.md</files>
  <action>
Insert detailed command reference sections into README.md AFTER the `### wt init` subsection and BEFORE the `## Tab Completion` section. Add `wt new` and `wt eject` first (the two commands with flags).

**`wt new` (DOC-03):**
- Subsection: `### wt new`
- Usage line: `wt new [--copy <path>] [--symlink <path>] <name> [branch]`
- Description: Creates a new git worktree as a sibling directory. The worktree directory is named `<repo>-<name>` and placed alongside the main repo directory.
- Arguments:
  - `<name>` (required): Short name for the worktree. Used as directory suffix and default branch name.
  - `[branch]` (optional): Branch name to use. Defaults to `<name>`. If the branch already exists, it checks it out; otherwise creates a new branch.
- Flags:
  - `--copy <path>`: Copy the specified file or directory from the source worktree instead of following .wtconfig default. Can be repeated.
  - `--symlink <path>`: Symlink the specified file or directory from the source worktree instead of following .wtconfig default. Can be repeated.
- Behavior details:
  - Runs `_wt_setup` after creating the worktree, which processes `.wtconfig` (if it exists) and applies any `--copy`/`--symlink` overrides.
  - Flag overrides take precedence over `.wtconfig` entries for the same path.
  - Flags can also specify paths NOT in `.wtconfig` for one-off operations.
  - After creation, automatically `cd`s into the new worktree.
- Examples:
  ```zsh
  # Create worktree with new branch "feature-auth"
  wt new feature-auth

  # Create worktree with existing branch
  wt new hotfix release/hotfix-1.2

  # Override .wtconfig: copy .env instead of symlinking
  wt new feature-auth --copy .env

  # One-off: symlink node_modules (even if not in .wtconfig)
  wt new feature-auth --symlink node_modules

  # Multiple overrides
  wt new feature-auth --copy .env --symlink node_modules
  ```

**`wt eject` (DOC-04):**
- Subsection: `### wt eject`
- Usage line: `wt eject [--copy <path>] [--symlink <path>] [name]`
- Description: Moves the current branch out of the current worktree into a new dedicated worktree. Useful when you started work on a branch in the main worktree and want to isolate it.
- Arguments:
  - `[name]` (optional): Name for the new worktree directory. Defaults to the branch name with `/` replaced by `-`.
- Flags: Same as `wt new` (`--copy`, `--symlink`).
- Behavior details:
  - Stashes any uncommitted changes (including untracked files).
  - Switches the current worktree back to a fallback branch (main/master for home worktree, or the worktree's original branch for non-home worktrees).
  - Creates a new worktree for the ejected branch.
  - Restores stashed changes in the new worktree.
  - Processes `.wtconfig` and applies flag overrides (same as `wt new`).
  - After ejection, automatically `cd`s into the new worktree.
- Examples:
  ```zsh
  # Eject current branch into its own worktree
  wt eject

  # Eject with a custom worktree name
  wt eject my-feature

  # Eject with file overrides
  wt eject --copy .env.local --symlink node_modules
  ```
  </action>
  <verify>
- README.md contains "### wt new" section with usage, args, flags, and examples
- README.md contains "### wt eject" section with usage, args, flags, and examples
- grep "### wt new" README.md succeeds
- grep "### wt eject" README.md succeeds
- Both sections mention --copy and --symlink flags
- Both sections have code block examples
  </verify>
  <done>
`wt new` is fully documented with all flags, positional args, override behavior, and 5 examples (DOC-03). `wt eject` is fully documented with stash/fallback behavior and 3 examples (DOC-04).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add wt goto, wt home, and wt list command reference</name>
  <files>README.md</files>
  <action>
Insert `wt goto`, `wt home`, and `wt list` sections into README.md AFTER the `### wt eject` subsection and BEFORE `## Tab Completion`.

**`wt goto` (DOC-05):**
- Subsection: `### wt goto`
- Usage: `wt goto <worktree>`
- Description: Navigate to a worktree by name. Resolves the name by matching the directory suffix (e.g., `wt goto staging` matches `myapp-staging`).
- Example:
  ```zsh
  wt goto feature-auth
  ```

**`wt home` (DOC-05):**
- Subsection: `### wt home`
- Usage: `wt home`
- Description: Navigate back to the main (first) worktree. Useful when working in a secondary worktree and wanting to return to the primary one.
- Example:
  ```zsh
  wt home
  ```

**`wt list` (DOC-05):**
- Subsection: `### wt list`
- Usage: `wt list`
- Description: List all worktrees with their directory names and branches. The current worktree is marked with `*`.
- Example output:
  ```
  * myapp                          main
    myapp-feature-auth             feature-auth
    myapp-hotfix                   hotfix
  ```
  </action>
  <verify>
- README.md contains "### wt goto", "### wt home", "### wt list" sections
- grep "### wt goto" README.md succeeds
- grep "### wt home" README.md succeeds
- grep "### wt list" README.md succeeds
  </verify>
  <done>
Navigation and listing commands documented (DOC-05): `wt goto` with name resolution explanation, `wt home` with description, `wt list` with example output showing asterisk marker.
  </done>
</task>

</tasks>

<verification>
- README.md contains subsections for all 5 commands: wt new, wt eject, wt goto, wt home, wt list
- wt new section documents --copy and --symlink flags with override precedence explanation
- wt eject section explains stash/fallback/restore behavior
- wt goto explains name resolution (suffix matching)
- wt list shows example output format
- All sections use consistent format: usage line, description, examples
- No sections reference future content that doesn't exist yet (configuration details can forward-reference .wtconfig section from plan 03)
</verification>

<success_criteria>
- DOC-03 (wt new): All flags, positional args, override behavior, 5+ examples
- DOC-04 (wt eject): All flags, stash/fallback behavior, 3+ examples
- DOC-05 (wt goto, wt home, wt list): All three commands documented with examples
- Command sections are placed in logical order within the Commands section
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-facing-documentation/02-02-SUMMARY.md`
</output>
